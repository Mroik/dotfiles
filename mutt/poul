set smtp_pass=`kwallet-query -f Passwords -r poul default 2>/dev/null`
set imap_pass=`kwallet-query -f Passwords -r poul default 2>/dev/null`
set folder      = imaps://poul.org/
set imap_user   = mroik@poul.org
set spoolfile   = +INBOX
set from = "mroik <mroik@poul.org>"

set record="=Sent"
set copy=yes

set smtp_url=smtps://mroik@poul.org@poul.org
set ssl_force_tls = yes

set header_cache=~/.cache/muttheaders/poul
